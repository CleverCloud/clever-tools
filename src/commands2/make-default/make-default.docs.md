# 📖 `clever make-default` command reference

## ➡️ `clever make-default` <kbd>Since 0.5.0</kbd>

Make a linked application the default one

```bash
clever make-default <app-alias>
```

### 📥 Arguments

|Name|Description|
|---|---|
|`app-alias`|Application alias|
