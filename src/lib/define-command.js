/**
 * Define a CLI command with full type inference from Zod schemas.
 *
 * @type {import('./define-command.types.js').defineCommand}
 */
export function defineCommand(definition) {
  return definition;
}
