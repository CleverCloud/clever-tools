Closes #

## Context

<!--
Why this PR exists:
- Bug: what goes wrong today.
- Feature: what users cannot do today, or what is painful.
-->

## Proposal

<!-- What this PR fixes or adds: new commands, options or output, and the design decisions worth reviewing. -->

## How to test

<!-- Commands to run, with the expected output: before/after for a bug, main use cases and edge cases for a feature. -->

<!--
Before requesting a review:
- The PR stays focused on the linked issue
- `npm run validate` passes
- For CLI behaviour changes, the affected commands were run with `node bin/clever.js`
- Commits follow Conventional Commits, without fixup!/squash! commits, rebased on master
- If a command changed: `npm run docs` was run and generated files are committed
- If a command was added: it uses `since: null`
-->
